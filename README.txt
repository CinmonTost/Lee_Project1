The program is made using only one file, so compilation and running it should be easy.
I decided to use Java because it's the only language I know. I used switch statements to 
easily choose between the selected signed representations and multiple toString() methods
to make printing easier. 